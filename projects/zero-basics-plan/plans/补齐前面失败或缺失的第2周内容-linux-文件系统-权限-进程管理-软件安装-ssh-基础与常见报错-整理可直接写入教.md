# 补齐前面失败或缺失的第2周内容：Linux 文件系统、权限、进程管理、软件安装、SSH 基础与常见报错，整理可直接写入教程的步骤和练习

Generated: 2026-03-26

## Goal Description
补齐 28 天文档中缺失或失败的第 2 周内容，围绕 Linux 文件系统、权限管理、进程管理、软件安装、SSH 基础与常见报错，产出一套可直接写入教程的结构化内容。该内容需同时满足以下目标：  
1. 对零基础读者友好，具备清晰的概念说明、可复制执行的命令步骤、示例输出说明。  
2. 每个主题都包含“是什么、为什么、怎么做、常见坑、练习题”五类内容，避免只堆命令不解释。  
3. 并行补查前面失败记录中遗漏的点，确保知识链路完整，不与前后天内容冲突。  
4. 产出形式应可直接落入教程文档，无需大幅二次改写。  
5. 对 SSH 常见报错提供可复现的场景、排查顺序和修复建议，降低读者卡住概率。  

## Acceptance Criteria

- AC-1: 第 2 周缺失主题被完整覆盖，至少包含 Linux 文件系统、权限、进程管理、软件安装、SSH 基础与常见报错五大模块
  - Positive Tests: 文档目录中可清晰看到五个模块；每个模块均有概念、操作步骤、示例命令、注意事项、练习；读者按模块学习不会出现主题空缺
  - Negative Tests: 只补充部分模块；某模块仅有概念没有步骤；只有命令列表没有解释；主题之间出现明显断层

- AC-2: 每个模块都提供可直接写入教程的步骤化内容，命令顺序清晰、示例可执行
  - Positive Tests: 每个模块至少提供一组从环境确认到操作验证的完整步骤；命令可以在常见 Linux 发行版场景中理解和复用；示例具备输入与预期现象说明
  - Negative Tests: 步骤缺失前置条件；命令顺序混乱；示例依赖未说明的环境；无法判断执行结果是否正确

- AC-3: Linux 文件系统部分覆盖核心路径、目录操作、文件查看与查找、绝对路径与相对路径
  - Positive Tests: 涵盖 /、/home、/etc、/var、/tmp、/usr 等常见目录用途；包含 pwd、ls、cd、mkdir、touch、cp、mv、rm、find、cat、less 等基础命令；解释绝对路径与相对路径差异并附练习
  - Negative Tests: 只罗列目录名不讲用途；缺少路径概念；危险命令无提示；没有查找与查看文件的内容

- AC-4: 权限部分覆盖用户/用户组、rwx 权限、chmod、chown、可执行权限与常见权限报错
  - Positive Tests: 解释文件与目录权限差异；展示 ls -l 输出拆解；包含 chmod 数字法或符号法示例；包含 chown/chgrp 基础；包含 Permission denied 的成因与排查
  - Negative Tests: 未区分文件权限和目录权限；只给 chmod 命令不解释；遗漏 sudo 使用边界；未说明权限修改风险

- AC-5: 进程管理部分覆盖进程查看、筛选、结束、前后台概念与服务状态查看
  - Positive Tests: 包含 ps、top 或 htop、grep、kill、kill -9、jobs、bg、fg、&、systemctl status 等内容；给出正常结束与强制结束的区别；附带练习
  - Negative Tests: 只有 kill 命令没有查看方法；未解释 PID；未区分普通进程与系统服务；没有安全提示

- AC-6: 软件安装部分覆盖至少两类主流包管理器及基础安装/更新/卸载流程
  - Positive Tests: 至少覆盖 apt 与 yum/dnf 中两类；说明 update、install、remove、search、version 检查；解释 root/sudo 权限需求；给出安装 curl、git 或 tree 等常见工具示例
  - Negative Tests: 只讲单一发行版；命令不区分不同系统；缺少安装验证；未提示网络源问题或权限问题

- AC-7: SSH 基础部分覆盖连接命令、端口、用户名、密钥与密码登录差异、known_hosts 基础
  - Positive Tests: 包含 ssh user@host、-p 指定端口、首次连接确认、公钥/私钥概念、~/.ssh 目录基础；说明为什么推荐密钥登录；包含最小化安全提示
  - Negative Tests: 没有解释 user@host 结构；忽略端口场景；将私钥泄露风险一笔带过；没有首次连接提示说明

- AC-8: SSH 常见报错部分至少覆盖连接超时、连接被拒绝、权限拒绝、主机指纹变化等常见问题
  - Positive Tests: 包含 “Connection timed out”“Connection refused”“Permission denied”“REMOTE HOST IDENTIFICATION HAS CHANGED”等报错；每个报错都有可能原因、排查步骤、修复建议；排查顺序从网络、端口、服务、账号、密钥逐步展开
  - Negative Tests: 只有报错翻译没有处理办法；缺少复现条件；建议直接删除安全校验而不解释风险；排查顺序混乱

- AC-9: 教程内容具备练习设计，每个模块至少提供 2 个练习，且至少包含 1 个带参考答案或验证方式的练习
  - Positive Tests: 每个模块有操作练习；练习能在本地或虚拟机环境完成；至少部分练习提供预期结果或自检方法；难度从基础到稍进阶递进
  - Negative Tests: 只有阅读题没有实操；练习过于开放无法验证；没有答案或验证标准；依赖未提供的远程服务器

- AC-10: 产出与已有 28 天文档保持风格一致，并明确嵌入到第 2 周的天级结构中
  - Positive Tests: 输出按天或按小节可直接插入现有文档；标题层级统一；术语风格一致；可标记“第 X 天”对应内容
  - Negative Tests: 产出是零散笔记；没有考虑插入位置；风格明显脱离现有文档；与前后周内容重复严重

## Path Boundaries

### Upper Bound (Maximum Scope)
最完整方案为：  
在不重写全项目的前提下，完整补齐第 2 周教程为一个可直接发布的教学单元。按“天”拆分内容，例如：  
- 第 8 天：Linux 文件系统与路径  
- 第 9 天：文件操作与查找  
- 第 10 天：权限与用户组  
- 第 11 天：进程与任务控制  
- 第 12 天：软件安装与包管理  
- 第 13 天：SSH 基础连接与密钥  
- 第 14 天：SSH 常见报错排查与综合练习  

每一天都包含：学习目标、知识讲解、可复制命令步骤、示例输出、常见误区、练习题、参考答案/验证方式。  
同时补做以下增强项：  
1. 对 apt、yum、dnf 做发行版差异提示。  
2. 对权限与 SSH 问题加入“风险提示”和“不要这么做”的错误示范。  
3. 为每个主题提供一个最小实验环境说明，如 Ubuntu/Debian 或 CentOS/RHEL 系场景。  
4. 增加“常见报错速查表”和“命令对照表”。  
5. 对前面失败记录做映射，标出“已补齐的遗漏点”。  
6. 在第 2 周末加入综合练习：从创建目录、设置权限、安装工具到通过 SSH 连接并排查问题，形成闭环任务。  

### Lower Bound (Minimum Scope)
最简可行方案为：  
补齐一个紧凑版第 2 周文档，至少包含五大模块，每个模块必须具备：  
1. 简短概念说明；  
2. 至少 1 组可执行步骤；  
3. 至少 3 个核心命令；  
4. 至少 1 个常见报错或坑点；  
5. 至少 2 个练习，其中 1 个附验证方式。  

若时间或资料受限，可不做完整“按天重构”，但必须保证内容可直接插入第 2 周教程，并且足够让小白按步骤完成基础学习。

### Allowed Choices
- Can use: 现有 28 天文档结构、前面失败记录、Linux 常识资料、Ubuntu/Debian 与 CentOS/RHEL 系常见命令差异说明、虚拟机或本地 Linux 环境作为示例、教程式 Markdown 编排、命令示例与预期输出说明、报错案例归纳
- Cannot use: 依赖未说明的私有环境、跳过核验直接拼接旧内容、只给概念不提供操作步骤、只给命令不解释风险、要求读者必须具备云服务器或付费资源、用模糊占位符代替关键命令、把高风险操作写成默认推荐方案、忽略前面失败记录中的遗漏点

## Dependencies and Sequence

### Milestones
1. 盘点缺口与失败记录
   - 收集前面失败或缺失的第 2 周内容清单
   - 标记已经存在、部分存在、完全缺失的主题
   - 确认现有 28 天文档的标题层级、语气风格、天数分配方式

2. 确认第 2 周知识结构与分天方案
   - 设计五大模块与天级安排的映射关系
   - 明确每一天的学习目标、先后依赖和难度递进
   - 避免与前后周内容重复或冲突

3. 补查 Linux 文件系统与基础文件操作内容
   - 整理目录结构、路径概念、常用目录说明
   - 编写 pwd、ls、cd、mkdir、touch、cp、mv、rm、cat、less、find 等步骤
   - 添加危险操作提示与练习

4. 补查权限与用户组内容
   - 编写 ls -l 结构拆解、rwx 含义、文件/目录权限差异
   - 编写 chmod、chown、chgrp、sudo 基础使用
   - 补充 Permission denied 等常见问题与排查流程

5. 补查进程管理与任务控制内容
   - 编写 ps、top/htop、grep、kill、kill -9、jobs、bg、fg、&、systemctl status 等内容
   - 说明 PID、前后台任务、普通进程与服务区别
   - 配置练习题与安全提示

6. 补查软件安装与包管理内容
   - 编写 apt 与 yum/dnf 两类包管理器的安装、更新、搜索、卸载流程
   - 增加安装后验证命令
   - 说明 sudo、网络源、权限等常见失败原因

7. 补查 SSH 基础与常见报错内容
   - 编写 ssh 连接命令、端口、用户名、首次连接提示、known_hosts、密钥基础
   - 汇总常见报错并整理排查顺序
   - 形成“现象—原因—排查—修复”的教程结构

8. 统一整理为可直接写入教程的成稿
   - 统一 Markdown 层级、措辞风格和示例格式
   - 为每个模块加入练习与参考答案/验证方式
   - 补充小结、术语表、速查表或注意事项

9. 自检与验收映射
   - 按验收标准逐条检查是否覆盖
   - 确认所有命令示例具备上下文与验证方式
   - 确认第 2 周整体完整，可直接并入 28 天文档

## Implementation Notes
- 代码中不应包含 AC-、Milestone 等计划术语
- 优先按“可直接复制进教程”的方式撰写，而不是先写调研摘要
- 命令示例应尽量采用小白可复现的安全场景，避免默认使用破坏性命令
- 对高风险命令如 `rm -rf`、`chmod -R`、`kill -9` 必须附带风险提示与适用边界
- 如果存在发行版差异，应明确标注“Debian/Ubuntu 常用”和“CentOS/RHEL 常用”
- SSH 报错排查建议采用固定顺序：网络连通性、主机地址、端口、服务状态、防火墙、用户名、认证方式、密钥权限、主机指纹
- 练习设计应遵循“先基础、后综合”的顺序，并尽量提供自检方式，如查看输出、确认文件存在、确认权限变化、确认进程状态
- 若前面已有部分可用内容，应进行兼容性修订后吸收，不要机械重复
- 最终文本应具备可插入现有第 2 周文档的标题结构，例如按“第 X 天”或“模块 X”展开
- 对常见报错建议用统一模板整理：报错原文、典型原因、如何确认、如何修复、修复后如何验证
- 术语解释要简明统一，例如首次出现时解释“绝对路径、相对路径、PID、用户组、公钥、私钥、包管理器”等
- 示例命令中的路径、用户名、主机地址应使用清晰占位方式，并避免让读者误以为可直接替换生产环境配置